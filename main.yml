---
 - hosts: all
   tasks:
    - name:  linux file
      copy: 
          src: enlinuxpc64 
          dest: /opt/
    - name: install file
      path: /opt/enlinuxpc64 state=latest
      upgrade: yes
      update_cache: yes   
