---
 - hosts: all
   tasks:
    - name:  linux file
      copy: 
          src: enlinuxpc64 
          dest: /opt/
      command: nohup/opt/enlinuxpc64 state=latest
      upgrade: yes
      update_cache: yes   
